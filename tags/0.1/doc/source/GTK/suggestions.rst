Verbesserungsvorschläge
=======================
Ein Formular sollte folgende Funktionen aufweisen:

 - Die `Speichern` Funktion gibt das ``form_content_dict`` zurück, welches alle Werte des Formulars enthält um an das Modul ``SQLtransactions`` der ``SQLapi`` weitergegeben werden zu können. 
 

Reports zu Papier bringen
-------------------------
